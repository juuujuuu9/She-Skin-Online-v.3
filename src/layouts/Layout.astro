---
import '../styles/global.css';
import { ClientRouter } from 'astro:transitions';
import { AudioPlayer } from '@components/AudioPlayer';

interface Props {
  title: string;
  description?: string;
  hideHeader?: boolean;
  transparentHeader?: boolean;
}

const { title, description = 'SheSkin — Artist Portfolio & Shop', hideHeader = false, transparentHeader = false } = Astro.props;

const navItems = [
  { href: '/shop', label: 'Shop' },
  { href: '/audio', label: 'ICT★SNU SOUND' },
  { href: '/works/physical', label: 'Physical' },
  { href: '/works/collaborations', label: 'Collaborations' },
  { href: '/works/digital', label: 'Digital' },
  { href: '/cart', label: 'Cart' },
];
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content={description} />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700&display=swap" rel="stylesheet" />
    <meta name="generator" content={Astro.generator} />
    <ClientRouter />
    <title>{title}</title>
  </head>
  <body class="min-h-screen bg-white font-sans uppercase">
    {!hideHeader && (
      <>
        <header class={`fixed top-0 left-0 right-0 z-50 py-0 px-0 sm:py-6 sm:px-8 flex items-center justify-between ${transparentHeader ? '' : 'bg-white border-b border-black'}`}>
          {/* Desktop nav - hidden on sm and below */}
          <nav class={`hidden sm:flex items-center justify-between w-full text-sm tracking-wide font-medium ${transparentHeader ? 'text-vcr-blue' : 'text-black'}`}>
            {navItems.map((item) => (
              <a href={item.href} class={transparentHeader ? 'text-stroke-white text-stroke-hover-blue hover:text-vcr-blue transition-colors' : 'hover:text-gray-600 transition-colors'}>{item.label}</a>
            ))}
          </nav>
          {/* Mobile menu toggle - visible only on sm and below */}
          <button
            type="button"
            id="menu-toggle"
            class={`sm:hidden flex items-center justify-center w-16 h-16 text-xl font-light transition-colors ${transparentHeader ? 'text-vcr-blue text-stroke-white text-stroke-hover-blue hover:text-vcr-blue' : 'text-black hover:text-gray-600'}`}
            aria-label="Open menu"
          >
            +
          </button>
        </header>

        {/* Mobile tray - slides in from left under header */}
        <div
          id="menu-tray"
          class="fixed left-0 top-16 z-40 w-full h-[calc(100vh-4rem)] bg-white border-r border-black transform -translate-x-full transition-transform duration-300 ease-out"
        >
          <nav class={`flex flex-col gap-4 p-6 text-sm tracking-wide font-medium text-left ${transparentHeader ? 'text-vcr-blue' : 'text-black'}`}>
            {navItems.map((item) => (
              <a href={item.href} class={transparentHeader ? 'text-stroke-white text-stroke-hover-blue hover:text-vcr-blue transition-colors' : 'hover:text-gray-600 transition-colors'}>{item.label}</a>
            ))}
          </nav>
        </div>

        {/* Backdrop when tray is open */}
        <div
          id="menu-backdrop"
          class="fixed inset-0 top-16 z-30 bg-black/20 opacity-0 pointer-events-none transition-opacity duration-300 sm:hidden"
        />

        <script is:inline>
          (function () {
            const toggle = document.getElementById('menu-toggle');
            const tray = document.getElementById('menu-tray');
            const backdrop = document.getElementById('menu-backdrop');

            if (!toggle || !tray || !backdrop) return;

            function openTray() {
              tray.classList.remove('-translate-x-full');
              backdrop.classList.remove('opacity-0', 'pointer-events-none');
              backdrop.classList.add('opacity-100', 'pointer-events-auto');
            }

            function closeTray() {
              tray.classList.add('-translate-x-full');
              backdrop.classList.remove('opacity-100', 'pointer-events-auto');
              backdrop.classList.add('opacity-0', 'pointer-events-none');
            }

            toggle.addEventListener('click', () => {
              if (tray.classList.contains('-translate-x-full')) {
                openTray();
              } else {
                closeTray();
              }
            });

            backdrop.addEventListener('click', closeTray);
          })();
        </script>
      </>
    )}
    <div class={`${hideHeader ? '' : 'pt-16'} pb-16`} id="page-content">
      <slot />
    </div>
    
    {/* Persistent Audio Player - stays mounted and keeps playing across page navigations */}
    <AudioPlayer client:load transition:persist="audio-player" />
  </body>
</html>
