/**
 * Collaboration portfolio items (images + links) for the Collaborations works page.
 * Sourced from portfolio_list.txt / legacy sheskin.org list.
 */
import type { WorkGridItem } from '@components/WorksGrid';

import items from './collaborations.json';

export const collaborationsGridItems: WorkGridItem[] = items as WorkGridItem[];
